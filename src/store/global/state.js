export default function () {
  return {
    bkitserver: {}, // defulat bKit server as set on bBit-Client configuration default directory
    selectedServer: {}, // target server for current operations
    servers: []
  }
}
